# Implementation Plans

Generated by the improve skill (quick audit) on 2026-07-07, against commit
`ceacf8a`. No user was available to select findings, so the top finding by
leverage was planned by default (one plan — the repo is ~13 tracked files).

Repo status at audit time: **DORMANT code, LIVE service.**
`https://maptapdat.server.unarmedpuppy.com` returned HTTP 200 on 2026-07-07
with no auth, the DNS name is in the cloudflare-ddns DOMAINS list, and the
container runs from `home-server/apps/maptapdat/` (watchtower-enabled).
SECURITY-AUDIT-2026-07-02 line 138 assumed this service might be down
(`x-external` in compose) — **it is up**; that assumption should be
corrected in the tracker.

**Disposition recommendation**: the data ends 2025-12-09 (~7 months stale) —
the family apparently stopped playing. Two honest options:
(a) **retire**: stop container, remove `home-server/apps/maptapdat/`, remove
the DDNS entry, archive this repo in Gitea — zero salvage needed, the data
IS `data.csv` which is already committed; or
(b) **keep live as a family archive**: then execute plan 001 (auth split +
health), because family members' real names are currently on an
unauthenticated public URL. Do not keep the status quo.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | /health + EOL/off-Harbor base image fix + external-auth split (operator handoff) | P1 | S | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- None. If disposition = retire, mark 001 REJECTED with "service retired".

## Findings considered and rejected / settled

- **Two divergence-prone copies of `data.csv`** (this repo and
  `home-server/apps/maptapdat/data.csv`, mounted `:ro` into the container):
  real tech debt, but any fix (single source, ingestion endpoint) only makes
  sense under disposition (b); fold into the ingestion direction item below.
- **No tests**: ~900-line read-only aggregation server for family game
  scores; not worth a test harness. The /health check in plan 001 is the
  smoke test.
- **Google Fonts `@import` in `public/index.html`** (external dependency for
  a self-hosted app): cosmetic; not worth a plan.
- **`x-enabled`/privileged concerns from the 2026-07-02 security audit**: the
  audit's open question ("confirm they're actually down") is answered above —
  up, not down. The container itself is unprivileged and read-only-mounted;
  the only live risk is the missing auth, which is plan 001.

## Direction (for the maintainer)

- Grounded in `parse_imessage.py` / `parse_entries.py` (committed ingestion
  scripts that were run by hand): if the game ever resumes, the natural
  feature is automated ingestion — a small authed POST endpoint or a
  scheduled job running the parser, ending the manual
  commit-CSV-to-two-repos loop. Coarse effort: M. Only worth it under
  disposition (b) with active players.
