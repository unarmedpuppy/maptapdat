name: Build and Deploy

on:
  push:
    branches: [main, master]
    tags: ['v*']
  workflow_dispatch:

jobs:
  build:
    uses: homelab/workflows/.gitea/workflows/docker-build.yml@main
    with:
      image_name: library/maptapdat
      app_path: apps/maptapdat
    secrets: inherit
